<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sammy.oasisadminapp17">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChoicePage">

        </activity>
        <activity android:name=".AddValidateProf" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ValidatePage" />
        <activity android:name=".AddProfsPage" />
        <activity android:name=".Notifications" />
        <activity android:name=".EMSPage" />
        <activity android:name=".EMScan" />
        <activity android:name=".ProfsChoice1" />
        <activity android:name=".ProfsChoice2"></activity>
    </application>

</manifest>